name: PHPCSFixer

on:
  pull_request:
    branches:
      - main
    paths:
      - '**.php'
      - '.github/workflows/phpcsfixer.yml'
  push:
    branches:
      - main
    paths:
      - '**.php'
      - '.github/workflows/phpcsfixer.yml'

jobs:
  phpcsfixer:
    uses: codeigniter4/.github/.github/workflows/phpcsfixer.yml@main
